#!/usr/bin/env ruby

`vim +NeoBundleUpdate +qall &> /dev/null`

Dir['zsh/bundle/*'].each do |d|
  Dir.chdir(d) do
    `git pull`
  end
end

def push_to_github()
  `git add zsh/bundle`
  `git commit -m 'ZSH plugin update'`
  `git push`
end

push_to_github unless `git status` =~ /nothing to commit/
